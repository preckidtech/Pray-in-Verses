name: Deploy React + Tailwind to cPanel

on:
  push:
    branches:
      - main # Replace with your default branch if different

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1 — Checkout repository
      - name: Checkout code
        uses: actions/checkout@v4

      # Step 2 — Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22.19.0" # Replace with your Node.js version

      # Step 3 — Install dependencies
      - name: Install dependencies
        run: npm install

      # Step 4 — Build project
      - name: Build project
        run: npm run build

      # Step 5 — Deploy build folder via FTP
      - name: Upload build to cPanel via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.6
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./build
          server-dir: /public_html/
          protocol: ftp # Use 'sftp' if your host supports it
